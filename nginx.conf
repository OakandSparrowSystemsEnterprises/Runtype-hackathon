server {
    listen 8080;

    location = / {
        root /usr/share/nginx/html;
        try_files /day2-arena.html =404;
        default_type text/html;
    }

    location = /day2-arena.html {
        root /usr/share/nginx/html;
        default_type text/html;
    }

    location = /.well-known/ai-agent.json {
        root /usr/share/nginx/html;
        default_type application/json;
    }

    location = /health {
        proxy_pass http://host.docker.internal:8787;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location ^~ /api/demo/ {
        proxy_pass http://host.docker.internal:8083;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 30s;
    }

    location ^~ /api/v2/artifacts {
        proxy_pass http://host.docker.internal:8081;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location = /api/v2/actions/navigation {
        proxy_pass http://host.docker.internal:8082;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location / {
        default_type application/json;
        return 404 '{"error":"route_not_exposed"}';
    }
}
