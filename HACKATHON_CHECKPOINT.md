# Gatekeeper V2 Agent Native Builders Checkpoint

Updated: 2026-08-27
Branch: `day2-progressive-arena-2026-08-26`

The frozen judge invariant is the two-agent authority-transfer proof: raw bytes become a SHA-256/content-addressed immutable ArtifactRef; Agent A is authenticated but denied execution; the same ArtifactRef is handed to Agent B; `authority_transfer_from_artifact=false`; Agent B is independently authorized; Gatekeeper returns the real GREEN/permit result; the effect produces a sealed receipt. The visible line remains: **THE ARTIFACT MOVED. AUTHORITY DID NOT.** This is the minimal proof case, not the target architecture.

The target architecture is a swarm-native OASSE Deterministic Steward running substantive bounded compute on Tenki. The Steward decides what work the swarm should attempt and tracks deterministic state, provenance, timing, aggregation, and failure isolation. Gatekeeper remains the only authority boundary for effects. Cotal remains a coordination/handoff integration and must not be described as the OASSE Deterministic Steward.

Progressive Day 2 UX exists on this branch. The base `/api/demo/run` path returns the Gatekeeper authority-transfer result first and caches it by `run_id`; `/api/demo/evidence` is the slower evidence phase. Direct Gatekeeper latency remains the measured tens-of-milliseconds path (18.07 ms min, 22.70 ms p50, 24.65 ms mean, 36.45 ms p95, 48.29 ms max from the latest 50-run sample). The two-agent proof is about 240 ms. The prior full Arena orchestration was about 13.5 s. Arena wall time must never be labeled Gatekeeper latency.

Tenki remote checkpoint is recovered and must not be rebuilt unnecessarily: template `gatekeeper-goi-worker-v2`, template ID `01a04108-b541-7a1d-ba3d-e22c1479bca3`, successful build `01a0410a-b9fe-7c5b-a79f-65848181b2b3`, snapshot `07fd77b8-7caf-400e-8e8e-42eb16396098`, start command `python3 /home/tenki/gatekeeper-tenki/worker.py`. The remaining live step is to create a fresh Tenki sandbox from that successful snapshot, capture the actual worker response, then bind the real response shape without inventing it.

Repository-side Tenki work is in place: `src/demo-orchestrator/tenki_swarm.py` defines the OASSE-owned swarm plan contract, explicitly marks every worker result as non-authoritative, preserves the upstream live response verbatim under `raw`, rejects attempts to manufacture authority/permit/capability/Gatekeeper verdict, and stays `NEXT_PENDING` when no real Tenki Steward endpoint exists. `src/demo-orchestrator/evidence_pipeline.py` runs the existing slower Cotal/estate evidence and the Tenki swarm plane concurrently after the base verdict, reports `supporting_evidence_ms`, and keeps Deterministic Steward status `IMPLEMENTATION_PENDING` until the Tenki plane is genuinely live. `src/demo-orchestrator/server.py` routes `/api/demo/evidence` through that progressive pipeline while preserving the legacy `/api/demo/arena` compatibility path. Health output advertises the Tenki swarm contract but does not claim live Tenki execution.

Day 2 judge UI is wired to the progressive contract. `web/day2-arena.html` renders the Gatekeeper verdict and authority-transfer invariant first, then independently renders OASSE Deterministic Steward state, Tenki swarm compute, Cotal coordination evidence, and the five-domain estate. The UI only marks Tenki LIVE when `tenki.live=true`, `tenki.status=LIVE`, and `tenki.authority=false`; Steward is only LIVE when the backend Steward status is LIVE, authority is false, and Tenki is genuinely live. Otherwise both remain visibly NEXT/PENDING or IMPLEMENTATION PENDING. Planned or returned swarm workers are shown as bounded non-authoritative workers. Cotal is explicitly labeled separate from Steward and Gatekeeper authority.

Judge UI resilience is hardened in commit `2004817256e1fed35ce1b2983ba0319076d32dc9`. Supporting evidence now has a 20-second client-side timeout and a separate failure boundary after the Gatekeeper verdict. If `/api/demo/evidence` fails or hangs after the verdict is sealed, the page no longer turns the entire demo into `DEMO FAILED`; it keeps the Gatekeeper verdict green, preserves **THE ARTIFACT MOVED. AUTHORITY DID NOT.**, marks Cotal/estate evidence unavailable, leaves Tenki NEXT/PENDING and Steward IMPLEMENTATION PENDING, and explicitly states that supporting-evidence failure cannot create or erase authority. Failures before the Gatekeeper verdict remain clearly labeled `DEMO FAILED BEFORE VERDICT`.

Supporting-evidence failure isolation is hardened in commit `80e5e28583e3a1bf3fa30a752bb2cc7463f39482`. A Tenki runtime failure no longer erases already-resolved Cotal/estate evidence, and a Cotal/estate failure no longer suppresses the Tenki plane. The `/api/demo/evidence` envelope preserves the already-sealed Gatekeeper verdict with `gatekeeper_verdict_preserved=true`, reports `evidence_complete` separately, and keeps every failure object explicitly non-authoritative. Three targeted deterministic checks passed against the exact committed function body: normal NEXT_PENDING Tenki with healthy estate, Tenki exception isolation with healthy estate preserved, and Cotal/estate exception isolation with Tenki result preserved.

Signed-action 502 diagnosis is repository-backed. `scripts/diagnose_action_edge.py` independently probes action-edge health, the direct Gatekeeper `/health` path, the required `parent-shield` route mount, the public-edge unsigned `401 missing_agent_auth` boundary, and optionally a real signed action when the caller supplies an existing agent id, secret, and ArtifactRef through environment variables. The script never prints the signing secret. The currently verified topology means a public unsigned request reaching `401 missing_agent_auth` proves nginx reached action-edge; a later signed `502` therefore lies after edge authentication/capability handling, with the action-edge to `GATEKEEPER_BASE_URL` hop the primary unresolved boundary. No runtime fix is claimed until the diagnostic is executed against the live stack.

A fail-closed V2 source-checkout pin exists in `config/v2-source-pin.json` with verifier `scripts/verify_v2_source_pin.py`. The current verified source pin is Gatekeeper-V2-NPU commit `338a126521a8427fe5d1988d0a1381affe8c75bd`. The verifier exits nonzero if `GATEKEEPER_V2_SOURCE_ROOT` does not resolve to that exact commit. Its output explicitly says `pin_scope=source_checkout_only` and `runtime_identity_proven=false`; this does not overclaim that a running image/process is proven to be that commit.

Commits verified at this checkpoint: `38162bfef77e9b0ef0a69604098145a9eff468d2` Tenki swarm orchestration contract; `8f7685417df2ae6ba5a267c352f9c480d56050be` progressive evidence pipeline; `2ed79cf78a06950fa87c7551529e76c249390f97` progressive evidence route; `c874297b44c3ac36ebbda773764446369da7ffae` authority-boundary contract tests; `28f9b8b2ea454a4110815b255e8faad9f8c192ce` prior checkpoint; `7b2d47cd5fa3a5a83bd244fc8eb5502f2bcef595` progressive Tenki/Steward Day 2 UI; `9621a8e1ea703950d6a83819df46e2c53733e4af` prior UI checkpoint; `c47e238cf3c57436d50299936de3fe2e142098bc` signed-action diagnostic harness; `6b7242a77092f037692e1b25f969492bf97371f0` V2 source pin; `d6454684243fbb9fec59b06cf8a97a858c07cb0c` fail-closed source-pin verifier; `80e5e28583e3a1bf3fa30a752bb2cc7463f39482` supporting-evidence failure isolation; `2004817256e1fed35ce1b2983ba0319076d32dc9` judge UI verdict-preservation and evidence timeout.

Verification already established: four Tenki contract tests passed against committed logic: swarm plans never grant authority, a missing live endpoint reports `NEXT_PENDING`, an upstream response asserting authority is rejected, and a legitimate normalized response preserves the raw upstream payload while remaining non-authoritative. The Python diagnostic and source-pin verifier were syntax-compiled before commit. The progressive evidence isolation logic passed three targeted deterministic checks as described above. The Day 2 UI verdict-preservation change was re-fetched from the branch after commit and statically verified to keep the base verdict outside the evidence try/catch, apply a 20-second AbortController only to `/api/demo/evidence`, preserve the sealed verdict on evidence failure, and reserve full demo failure for pre-verdict errors. Gatekeeper-V2-NPU was inspected read-only; no V2 core files were changed. CI intentionally skipped to conserve GitHub Actions usage.

Current dependencies: live Tenki execution remains NEXT/PENDING until snapshot `07fd77b8-7caf-400e-8e8e-42eb16396098` is launched and its real worker response is captured. Public signed-action `502 Bad Gateway` remains unresolved pending execution of the diagnostic against the live stack. V2 source checkout pinning is READY, while runtime image/process identity proof remains PENDING because the current V2 health contract does not expose an immutable build identifier. The next live dependency-safe action is to run the signed-action diagnostic on the active stack and use its direct Gatekeeper health result to distinguish V2 process availability from a bad action-edge `GATEKEEPER_BASE_URL` topology.
