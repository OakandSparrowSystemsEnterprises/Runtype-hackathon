# Gatekeeper V2 Agent Native Builders Checkpoint

Updated: 2026-08-27. Branch: `day2-progressive-arena-2026-08-26`.

Gatekeeper-V2-NPU remains the source of truth and Runtype-hackathon remains the integration/demo shell. No Gatekeeper authority logic has been copied or reimplemented here. The frozen minimal judge invariant remains the two-agent authority-transfer proof: raw bytes become a SHA-256/content-addressed immutable ArtifactRef; Agent A is authenticated but denied execution; the same ArtifactRef moves to Agent B; `authority_transfer_from_artifact=false`; Agent B is independently authorized; Gatekeeper returns the real GREEN/permit result; the effect produces a sealed receipt. The visible line remains **THE ARTIFACT MOVED. AUTHORITY DID NOT.** This is the minimal proof only. The target architecture remains an OASSE Deterministic Steward coordinating a swarm of bounded workers while Gatekeeper independently governs every effect.

The Day 2 judge path is verdict-first. `/api/demo/run` returns the Gatekeeper authority-transfer result first and `/api/demo/evidence` resolves the slower supporting evidence afterward. The UI renders Gatekeeper before Tenki, Deterministic Steward state, Cotal, five-domain estate evidence, replay, chain, or security evidence. Supporting-evidence failure cannot erase an already-sealed Gatekeeper verdict. Direct Gatekeeper latency must remain distinct from orchestration latency: latest prior 50-run sample is 18.07 ms minimum, 22.70 ms p50, 24.65 ms mean, 36.45 ms p95, and 48.29 ms maximum; the complete two-agent proof was about 240 ms; prior full Arena orchestration was about 13.5 s. Commit `763a9697d91fe289de7c1c7ff971a2817f734539` exposes the successful action-edge -> V2 evaluate hop as `gatekeeper_upstream_latency_ms` without changing V2 or authority semantics. Commit `6d56fa52eaa4431bb394836a744711cbb37f31a3` measures the complete artifact ingress + Agent A deny + handoff + Agent B governed action duration in the demo orchestrator and returns both measurements under a distinct `latency` object. Commit `854273d94ef63ddf3172795d4a2c61d445c200b1` renders those two live measurements separately in the Day 2 UI and explicitly states that supporting Arena evidence time is not Gatekeeper latency.

Tenki repository integration is present and non-authoritative. The recovered known-good Tenki checkpoint is template `gatekeeper-goi-worker-v2`, template ID `01a04108-b541-7a1d-ba3d-e22c1479bca3`, successful build `01a0410a-b9fe-7c5b-a79f-65848181b2b3`, snapshot `07fd77b8-7caf-400e-8e8e-42eb16396098`, start command `python3 /home/tenki/gatekeeper-tenki/worker.py`. `src/demo-orchestrator/tenki_swarm.py` preserves live upstream worker output under `raw`, marks worker output non-authoritative, rejects attempts to manufacture authority, permit, capability, token, or Gatekeeper verdict, and reports `NEXT_PENDING` until a real live endpoint exists. `src/demo-orchestrator/evidence_pipeline.py` keeps Tenki in the slower evidence phase and isolates Tenki failures from Cotal/estate failures and vice versa. The live Tenki sandbox launch and capture of the actual GOI claim response remain PENDING. No live payload shape has been invented.

The Day 2 UI is progressive and truthful. `web/day2-arena.html` renders Gatekeeper first, preserves **THE ARTIFACT MOVED. AUTHORITY DID NOT.**, renders Tenki and Steward separately, keeps Cotal distinct from Steward and Gatekeeper, and only shows Tenki LIVE when `tenki.live=true`, `tenki.status=LIVE`, and `tenki.authority=false`. Steward remains IMPLEMENTATION PENDING unless the backend explicitly reports a live non-authoritative Steward state backed by live Tenki compute. A 20-second timeout applies only to supporting evidence. Evidence timeout or failure leaves the Gatekeeper verdict intact rather than turning the whole demo into failure. The UI also shows the current measured V2 evaluate hop and the current full A->B proof duration as separate values when the updated runtime is live.

The signed-action 502 track has advanced materially. Existing `scripts/diagnose_action_edge.py` proves separate public-edge, direct action-edge, and direct V2 boundaries. Commit `730125a2d10b6db78db1cc2fb2ad7dd3b42de7b5` adds `GET /health/upstream` to `src/action-edge/server.py`. That probe executes from the action-edge process namespace itself, calls the real V2 `/health` boundary through the effective `GATEKEEPER_BASE_URL`, verifies that `parent-shield` is mounted, reports upstream reachability and latency, and intentionally reports `runtime_identity_proven=false`. It does not expose `GATEKEEPER_BASE_URL`, credentials, signing keys, or Gatekeeper authority logic. Commit `205c341df6e7994bdbe4853cbec707d64de7135d` updates `scripts/diagnose_action_edge.py` to consume that in-process probe. Commit `2fc6c9d0c24f7587a31ac933562704053836effd` hardens the signed evaluation call itself so a successful HTTP hop with a malformed or non-JSON V2 response returns controlled `502 gatekeeper_invalid_response` instead of raising out of the handler and potentially surfacing as an opaque nginx 502. Commit `6509ff4e7809dad4dc3d4692fbfa09295c1742a4` updates the diagnostic to distinguish `gatekeeper_unreachable`, `gatekeeper_invalid_response`, and `gatekeeper_timeout` without printing secrets. The 502 is not marked fixed because these changes still require live execution against the active stack.

The fail-closed V2 source-checkout pin remains in `config/v2-source-pin.json` with verifier `scripts/verify_v2_source_pin.py`. The current verified source pin is Gatekeeper-V2-NPU commit `338a126521a8427fe5d1988d0a1381affe8c75bd`. The verifier proves only the source checkout and explicitly reports `runtime_identity_proven=false`. Immutable running image/process identity is still PENDING because the current V2 health contract does not expose a build identifier, and no V2 core change has been made to manufacture one.

Verified prior integration commits remain `38162bfef77e9b0ef0a69604098145a9eff468d2` for the Tenki swarm contract, `8f7685417df2ae6ba5a267c352f9c480d56050be` for progressive evidence, `2ed79cf78a06950fa87c7551529e76c249390f97` for `/api/demo/evidence`, `c874297b44c3ac36ebbda773764446369da7ffae` for authority-boundary tests, `7b2d47cd5fa3a5a83bd244fc8eb5502f2bcef595` for progressive judge UI, `80e5e28583e3a1bf3fa30a752bb2cc7463f39482` for evidence failure isolation, `2004817256e1fed35ce1b2983ba0319076d32dc9` for verdict-preserving UI failure handling, `c47e238cf3c57436d50299936de3fe2e142098bc` and `25653663d8db05b4ece817cf28f52b498e2c38e7` for signed-action diagnostics, and `6b7242a77092f037692e1b25f969492bf97371f0` plus `d6454684243fbb9fec59b06cf8a97a858c07cb0c` for source pinning.

Verification state remains conservative. The four original committed Tenki authority-boundary tests passed previously, and the progressive evidence isolation logic passed its three targeted deterministic checks. For the latency unit, the committed action-edge, demo-orchestrator, and Day 2 UI files were re-fetched from the target branch after write. Static verification confirms the action edge still calls the existing real V2 Parent Shield endpoint, successful requests expose only the measured V2 hop plus `runtime_identity_proven=false`, the orchestrator labels the full proof duration separately, and the UI never substitutes supporting evidence time for Gatekeeper latency. No live runtime verification has occurred, so the signed-action 502 remains PENDING rather than FIXED and the live latency fields remain READY FOR RUNTIME VERIFICATION. CI was intentionally skipped to conserve GitHub Actions usage and no no-op commit was created.

Commit `2bfaf55f60316ad7614335b70d7ce728f197e003` created `scripts/test_diagnose_action_edge.py`. The deterministic regression coverage locks the 502 classifier semantics without requiring the live stack: all healthy unsigned boundaries produce `READY_FOR_SIGNED_PROBE`; action-edge namespace failure isolates `action_edge_to_gatekeeper`; signed `gatekeeper_unreachable` and `gatekeeper_invalid_response` remain distinct; and a non-502 signed result is reported as `SIGNED_PROBE_COMPLETED` rather than being mislabeled `FIXED`. This adds no authority behavior and prints no secret material.

Current run added commit `42d2cc5466a213aabde8b1aa7948b1aab72c3e51`, hardening `src/demo-orchestrator/tenki_swarm.py` so the non-authority boundary is recursive rather than top-level only. Any nested worker, aggregate, or other Tenki/Steward object that attempts to assert `authority`, `permit`, `capability`, `token`, or `gatekeeper_verdict` with a value other than `null`/`false` is rejected before the result can be marked LIVE. The actual upstream payload is still preserved verbatim under `raw`; no live Tenki payload shape was invented or imposed. Commit `83f4771194bb4bede75dfe370cdee94fd9349cac` extends `src/demo-orchestrator/test_tenki_swarm.py` with regression coverage for a nested worker-issued token and a nested aggregate Gatekeeper verdict. These tests are committed for execution in the checked-out branch; this connector-only run does not claim local execution.

CI INTENTIONALLY SKIPPED TO CONSERVE GITHUB ACTIONS USAGE. The two changed Tenki files were fetched from the target branch before editing, and the resulting commits are narrow integration-shell changes only. Gatekeeper-V2-NPU was not modified.

The next dependency-safe live action remains to run `python3 scripts/diagnose_action_edge.py` against the active Day 2 stack after deploying the updated action edge. A `gatekeeper_unreachable` result points to action-edge process namespace or effective `GATEKEEPER_BASE_URL`; `gatekeeper_invalid_response` proves the evaluation hop connected but returned a non-decodable response; `gatekeeper_timeout` isolates evaluation latency; and a non-502/504 signed result proves only that the prior opaque failure did not reproduce. Once that live stack is up, the same run will also populate the measured V2 hop and full A->B proof timing in the Day 2 UI.